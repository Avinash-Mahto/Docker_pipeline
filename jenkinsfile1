pipeline {
    agent {
        docker {
            stages {
                stage { Build }
                steps {
                    sh 'cat /etc/os-release'
                }
            }
        }
    }
}
